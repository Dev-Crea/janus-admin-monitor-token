---

en:
  menu:
    tokens:
      button: Tokens
      dropdown:
        list: View all tokens
        add: Add a new token
  tokens:
    index:
      title_html: Tokens
      table:
        empty:
          text: No token yet.
          link: Add a new token.
        header:
          token_id: Token ID
    new:
      title: Add a new token
      label: Id to token (if empty auto generated)
    error:
      disabled_html: |
        Token based authentification disabled.
        <a href="https://janus.conf.meetecho.com/docs/auth.html">
        See Janus documentation</a>.
